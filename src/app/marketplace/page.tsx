'use client';
import PlaceholderPage from '@/components/PlaceholderPage';
import { FaStore } from 'react-icons/fa';

export default function MarketplacePage() {
  return <PlaceholderPage title="Marketplace" icon={FaStore} description="Browse and install new apps and overlays for your room." />;
}